name: Deploy to Staging

on:
  workflow_run:
    workflows: ["CI - Build & Test"]
    branches: [build]
    types: [completed]
  workflow_dispatch: # Allow manual trigger

jobs:
  deploy:
    name: Deploy to Staging
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build application
        run: pnpm build
        env:
          NODE_ENV: production
          DATABASE_URL: ${{ secrets.SMOKESHOP_DATABASE_URL }}
          INVENTORY_MANAGER_EMAILS: ${{ secrets.INVENTORY_MANAGER_EMAILS }}
          CLOVER_ACCESS_TOKEN: ${{ secrets.CLOVER_ACCESS_TOKEN }}
          CLOVER_MERCHANT_ID: ${{ secrets.CLOVER_MERCHANT_ID }}
          NEXT_PUBLIC_SITE_URL: https://staging.neutraldevelopment.com
          AGE_VERIFICATION_PROVIDER: dob-photo

      - name: Create deployment package
        run: |
          # Create a clean deployment directory
          mkdir -p deploy

          # Copy built application and dependencies from CI
          cp -r .next deploy/
          cp -r app deploy/
          cp -r lib deploy/
          if [ -d public ]; then cp -r public deploy/; fi
          cp -r node_modules deploy/
          cp package.json deploy/
          cp next.config.mjs deploy/
          cp tsconfig.json deploy/
          if [ -f middleware.ts ]; then cp middleware.ts deploy/; fi
          cp server.js deploy/
          cp restart.sh deploy/
          cp check-deployment.sh deploy/
          cp index.php deploy/
          cp .htaccess deploy/
          cp ecosystem.config.js deploy/

          # Update index.php to proxy to staging port 3001
          sed -i "s|http://127.0.0.1:3000|http://127.0.0.1:3001|g" deploy/index.php

          #Create .env.production
          echo "NODE_ENV=production" > deploy/.env.production
          echo "DATABASE_URL=${{ secrets.SMOKESHOP_DATABASE_URL }}" >> deploy/.env.production
          echo "INVENTORY_MANAGER_EMAILS=${{ secrets.INVENTORY_MANAGER_EMAILS }}" >> deploy/.env.production
          echo "CLOVER_ACCESS_TOKEN=${{ secrets.CLOVER_ACCESS_TOKEN }}" >> deploy/.env.production
          echo "CLOVER_MERCHANT_ID=${{ secrets.CLOVER_MERCHANT_ID }}" >> deploy/.env.production
          echo "CLOVER_WEBHOOK_SECRET=${{ secrets.CLOVER_WEBHOOK_SECRET }}" >> deploy/.env.production
          echo "ADMIN_API_SECRET=${{ secrets.ADMIN_API_SECRET }}" >> deploy/.env.production
          echo "NEXT_PUBLIC_SITE_URL=https://staging.neutraldevelopment.com" >> deploy/.env.production
          echo "AGE_VERIFICATION_PROVIDER=dob-photo" >> deploy/.env.production
          echo "PORT=3001" >> deploy/.env.production

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -p ${{ secrets.VPS_SSH_PORT }} -H ${{ secrets.VPS_SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to VPS Staging Path
        run: |
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/id_ed25519 -p ${{ secrets.VPS_SSH_PORT }}" \
            deploy/ \
            ${{ secrets.VPS_SSH_USER }}@${{ secrets.VPS_SSH_HOST }}:/opt/smokeshop/smokeshop-backend/

      - name: Set file permissions on server
        run: |
          ssh -i ~/.ssh/id_ed25519 -p ${{ secrets.VPS_SSH_PORT }} \
            ${{ secrets.VPS_SSH_USER }}@${{ secrets.VPS_SSH_HOST }} \
            "chmod -R 755 /opt/smokeshop/smokeshop-backend"

      - name: Restart application
        run: |
          ssh -i ~/.ssh/id_ed25519 -p ${{ secrets.VPS_SSH_PORT }} \
            ${{ secrets.VPS_SSH_USER }}@${{ secrets.VPS_SSH_HOST }} \
            "cd /opt/smokeshop/smokeshop-backend && chmod +x restart.sh && bash restart.sh"

      - name: Verify deployment
        run: |
          echo "Waiting for application to start..."
          sleep 10

          # Check if site is accessible
          response=$(curl -s -o /dev/null -w "%{http_code}" https://staging.neutraldevelopment.com)
          if [ $response -eq 200 ] || [ $response -eq 301 ] || [ $response -eq 302 ]; then
            echo "✅ Staging deployment successful - Site is accessible"
          else
            echo "⚠️ Warning: Staging site returned HTTP $response"
          fi

      - name: Deployment summary
        run: |
          echo "🚀 Staging deployment completed successfully!"
          echo "📦 Build: $(cat deploy/.next/BUILD_ID)"
          echo "🌐 URL: https://staging.neutraldevelopment.com"
          echo "⏰ Time: $(date)"
